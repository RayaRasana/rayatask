import { SupportedLanguages } from '../../types';
import { convertDigits } from './convert-digits';
export declare type ConvertDigitsInput = string | any | number;
export interface ConvertDigitsOptions {
    to?: SupportedLanguages;
    separator?: string | boolean;
}
export declare const convertDigitsDefaultOptions: ConvertDigitsOptions;
export declare const defaultSeparator = "\u066C";
export default convertDigits;
