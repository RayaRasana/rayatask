import { ICalendarProps } from '@react-shamsi/calendar';
import React from 'react';
interface DatePickerOnChange {
    onChange?: (newDate: Date, fromDate?: Date) => void;
}
interface IDatePickerProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, "onChange">, DatePickerOnChange {
    autoUpdate?: boolean;
    defaultDate?: Date | [Date, Date];
    calendarProps?: ICalendarProps;
    date?: Date | [Date, Date];
    to?: Date;
    dateFormat?: string;
    persianDigits?: boolean;
    experimental_ranged?: boolean;
    calendarPortalElement?: HTMLElement | null;
}
declare const DatePicker: ({ autoUpdate, calendarProps, onChange, defaultDate, dateFormat, date: controlledDate, to, persianDigits, calendarPortalElement, experimental_ranged: ranged, ...props }: IDatePickerProps) => JSX.Element;

export { DatePicker };
