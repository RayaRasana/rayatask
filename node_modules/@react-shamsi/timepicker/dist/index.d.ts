declare type TTimePickerTheme = {
    backgroundColor: string;
    clockBackgroundColor: string;
    clockLabelsColor: string;
    pointerBackgroundColor: string;
    amPmColor: string;
    amPmActiveBackgroundColor: string;
};
interface ITimePickerProps {
    theme?: TTimePickerTheme;
    onChange?: (hour: number, minute: number) => void;
}
declare const defaultTimePickerTheme: TTimePickerTheme;
declare const TimePicker: ({ onChange, theme, }: ITimePickerProps) => JSX.Element;

export { TTimePickerTheme, TimePicker, defaultTimePickerTheme };
